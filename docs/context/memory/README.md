# Carried-over memory

These are copies of the durable memory notes from the session/machine that created Cellect. A new
Claude Code session doesn't inherit them automatically, so they're included here.

- `cellect_project.md` — Cellect's status, decisions, and plan (project-specific).
- `spacr_commit_attribution.md` — the user is sole author; never add a `Co-Authored-By` trailer.
- `spacr_no_tensorflow.md` — never introduce TensorFlow-backed code.
- `spacr_workflow.md` — never push without approval; small, named commits.
- `spacr_versioning.md` — versioning cadence preference.

(The `spacr_*` notes originate from the user's other project but encode general working
preferences that apply here too.)

**To re-seed into this session's memory:** ask Claude to *"read docs/context/memory/*.md and save
the ones that still apply to your memory."* The most important, applied directly, are: sole-author
commits (no Co-Authored-By), no TensorFlow, small named commits, don't push without approval.
